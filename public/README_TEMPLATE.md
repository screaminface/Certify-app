# Certificate Template Setup

## Как да настроя template.docx файла

1. Създай Word документ с име `template.docx`
2. Постави го в `/public/` папката
3. Използвай следните placeholders в документа:

### Достъпни полета:

- `{F01}` - Номер на сертификат (uniqueNumber)
- `{F02}` - Дата на курса (courseEndDate във формат DD.MM.YYYY)
- `{F03}` - Пълно име на курсиста
- `{F04}` - ЕГН (ако се добави към базата данни)
- `{F05}` - Място на раждане (ако се добави към базата данни)
- `{F06}` - Гражданство (по подразбиране "българско")
- `{F07}` - Година (извлича се от courseEndDate)
- `{F08}` - Номер на протокол (groupNumber)
- `{F09}` - Оценка текст ("Отличен" или "Мн. добър") - генерира се случайно
- `{F10}` - Оценка цифра ("6" или "5") - генерира се случайно
- `{F11}` - Дата на курса (същото като F02)

### Пример:

```
УДОСТОВЕРЕНИЕ № {F01}

Издадено на {F02} г.

Удостоверява се, че {F03} завърши успешно курс...

Оценка: {F09} ({F10})

Протокол № {F08} от {F11} г.
```

## Как работи:

1. Отвори списъка с участници
2. Намери участника за когото искаш сертификат  
3. Натисни зеления бутон с икона на документ (📄)
4. Сертификатът ще се изтегли автоматично

## Забележки:

- Оценките се генерират случайно (50% "Отличен"/6, 50% "Мн. добър"/5)
- Ако искаш да добавиш ЕГН, място на раждане и др., трябва да разшириш Participant интерфейса
